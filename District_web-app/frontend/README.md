
  # DistrictConnect Web App

  This is a code bundle for DistrictConnect Web App. The original project is available at https://www.figma.com/design/hmXaS9mZNFmpjuoJxMl8ic/DistrictConnect-Web-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  